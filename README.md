# ✍️ Text-to-Handwriting Conditional GAN

This project trains a Conditional GAN (cGAN) to generate realistic handwritten notes from typed text.

## 📦 Features
- Conditional GAN (Generator + Discriminator)
- Text embedding via GRU encoder
- IAM dataset preprocessing
- Modular PyTorch training pipeline
- Configurable hyperparameters via YAML

## 🚀 Training
```bash
python src/train.py
# Handwriting_001
